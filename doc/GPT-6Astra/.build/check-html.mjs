import {chromium} from 'playwright';
const browser=await chromium.launch({headless:true,channel:'msedge'});
const page=await browser.newPage({viewport:{width:1440,height:1000}});
const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto('file:///C:/DATA/personal/ai-tools/doc/GPT-6Astra/show-me-astra.html');
await page.getByRole('button',{name:'关键证据缺失',exact:true}).click();
if(!await page.locator('#path').innerText().then(t=>t.includes('请求关键输入')))throw Error('Flow failed');
await page.getByRole('button',{name:'没有板卡',exact:true}).click();
if(!await page.locator('#path').innerText().then(t=>t.includes('标记未验证')))throw Error('Hardware flow failed');
await page.getByRole('button',{name:'复制 Prompt',exact:true}).click();
if(!await page.locator('#status').innerText())throw Error('Copy status missing');
await page.screenshot({path:'C:/DATA/personal/ai-tools/doc/GPT-6Astra/.build/html-desktop.png',fullPage:true});
await page.setViewportSize({width:390,height:844});
if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth))throw Error('Mobile overflow');
await page.screenshot({path:'C:/DATA/personal/ai-tools/doc/GPT-6Astra/.build/html-mobile.png',fullPage:true});
await browser.close();if(errors.length)throw Error(errors.join('\n'));console.log('HTML: flows, clipboard response, mobile width and script errors checked');
