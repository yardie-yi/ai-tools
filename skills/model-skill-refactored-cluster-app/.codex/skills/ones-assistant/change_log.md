# Change Log — ones-assistant

> 本文件由 skill-refiner 自动维护。新条目追加在顶部（最新在上）。

## 2026-08-11 MCP 缺失时先尝试自动配置

- **问题**：ONES MCP 工具不可用时，skill 立即停止并要求用户自行配置，没有检查本机是否已有可复用的连接信息。
- **纠正**：先识别当前客户端、复用可信的本地 ONES 配置、自动注册并做只读连接验证；只有确实缺少地址或验证失败时才中断并询问用户。
- **根因分类**：flow, detail
- **WHY**：工具未加载不等于服务器地址不存在。自动复用已有可信配置能够完成正常前置恢复，避免把可自动处理的问题转交给用户，同时仍通过脱敏和只读验证保护凭据与外部数据。
- **改动文件**：
  - `SKILL.md` — 将“立即停止”改为发现、配置、验证、必要时再阻塞的恢复流程。
  - `README.md` — 补充 Codex 配置命令、已有配置复用规则和连接验证步骤。
  - `change_log.md` — 记录本次用户纠正及修改原因。
- **会话摘要**：调用 ones-assistant 分析 Bug 时因当前任务没有 ONES 工具而停止；随后发现本机 Claude 配置已有项目 ONES 地址，将其迁移至 Codex 并通过 `who_am_i` 验证成功。
